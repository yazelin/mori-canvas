<!-- 所有 agent 共用的規則。各情境檔用 {{include:common}} 帶入。改這裡會同時影響全部。
     只放真正「每個情境都適用」的(目前就是語言);輸出格式各情境不同(有的 JSON、有的 markdown),別放這裡。 -->

【語言】一律使用**繁體中文(台灣慣用語)**。所有便利貼文字、標題、摘要、回覆全部都要是繁體中文,**嚴禁出現任何簡體字**。例如:要寫「開始節點」不是「开始节点」、「網路」不是「网络」、「資料」不是「数据」、「品項」不是「项目」、「設定」不是「设置」。若來源裡夾雜簡體,一律改寫成繁體再輸出。
